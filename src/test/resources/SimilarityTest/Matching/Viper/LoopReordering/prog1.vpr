method doubleLoop(n: Int) returns (res: Int)
{
  res := 0
  var i: Int := 0;
  var j: Int := 0;
  while(i <= n)
  {
      while(j <= n)
      {
        res := res + i + j
        j := j + 1
      }
      i := i + 1
  }

}
